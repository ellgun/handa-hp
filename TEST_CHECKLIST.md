# TEST_CHECKLIST.md — 한다뚝딱 (handa. 뚝딱)

> **적용 단계 안내**
> 이번 버전은 로컬 실행이 기준이며, 모든 외부 API(Google 로그인, Gemini/Stitch AI, Supabase Database/Storage, Gmail 발송)를 더미로 대체합니다.
> `[다음 단계]` 표시가 붙은 섹션은 더미로 대체된 부분이 실 서비스로 전환된 뒤 확인합니다.

---

## 프로젝트 실행

- [ ] 의존성 설치 성공 (`npm install`)
- [ ] 개발 서버 실행 성공 (`npm run dev`)
- [ ] 콘솔에 API 키 등 비밀값이 출력되지 않음
- [ ] `npm run build` 성공 (빌드 오류 없음)
- [ ] `.env` 파일이 `.gitignore`에 포함되어 있음

---

## 로그인 (01_Login)

- [ ] Google 로그인 버튼 클릭 시 로그인 처리(테스트 버전은 더미 로그인 상태로 대체) 성공
- [ ] 로그인 실패 시 오류 메시지 표시
- [ ] 로그인 중 버튼 중복 클릭 방지
- [ ] 로그인 성공 후 홈(02_Home)으로 자동 이동
- [ ] 로그아웃 작동
- [ ] 재방문 시 세션 유지 (자동 로그인)
- [ ] 비로그인 상태에서 `/input`, `/result`, `/mypage` 접근 시 로그인 페이지로 리디렉션

---

## 정보 입력 (03_Input)

- [ ] 필수 항목(업종·지역·매장명·연락처·메인문구·장점·색상·분위기·추가요구사항·이메일) 미입력 시 제출 차단
- [ ] 미입력 필드에 강조 표시 + 안내 메시지 출력
- [ ] 선택 항목(SNS 주소·사업자등록번호·벤치마킹 사이트) 빈칸으로 제출 가능
- [ ] 색상 테마 8가지 중 1개만 선택 가능
- [ ] 홈페이지 분위기 5가지 중 1개만 선택 가능
- [ ] 매장 사진 1~3장 업로드 및 미리보기 표시
- [ ] 사진 업로드 실패 시 안내 메시지 표시
- [ ] 입력값 임시 저장 (새로고침 후에도 유지)
- [ ] 이메일 형식 검증 (잘못된 형식 입력 시 경고)

---

## AI 처리 / 로딩 (04_Loading)

- [ ] 제출 후 로딩 화면으로 전환
- [ ] 단계별 진행 메시지 표시 ("분석 중...", "설계 중...", "완성 중...")
- [ ] 더미 시안 생성 처리 성공 (로컬 단계 — 실제 Gemini/Stitch AI 호출 대신 더미 응답 사용)
- [ ] 오류 상황 시뮬레이션 시 오류 메시지 + 재시도 버튼 표시
- [ ] 처리 완료 시 결과 화면(05_Result)으로 자동 이동
- [ ] 취소 버튼 클릭 시 입력 화면으로 복귀
- [ ] `[다음 단계]` Gemini API 실제 호출 성공
- [ ] `[다음 단계]` Stitch AI 시안 생성 실제 호출 성공

---

## 시안 결과 (05_Result)

- [ ] 생성된 홈페이지 시안이 미리보기로 표시됨
- [ ] 운영 제안 문구 카드 표시
- [ ] "이메일로 받기" 버튼 클릭 시 Gmail(Agentria API) 발송 요청(테스트 버전은 더미 발송 성공으로 대체)
- [ ] 이메일 발송 성공 안내 메시지 표시
- [ ] 이메일 발송 실패 시 안내 메시지 + 재시도 버튼 표시
- [ ] 발송 실패 후에도 시안이 마이페이지에 보존됨
- [ ] "마이페이지에 저장" 버튼 작동
- [ ] "다시 만들기" 버튼 클릭 시 입력 화면(03_Input)으로 이동
- [ ] DB에 이메일 본문이 저장되지 않음
- [ ] email_delivery_logs에 상태(status)만 저장됨

---

## 마이페이지 (06_MyPage)

- [ ] 로그인한 사용자의 이메일 표시
- [ ] 이전 생성 시안 히스토리 목록 표시
- [ ] 히스토리 항목에 업종·생성일시·이메일 발송 여부 표시
- [ ] 시안 항목 클릭 시 결과 화면(05_Result)으로 이동
- [ ] 이메일 재발송 버튼 작동
- [ ] 자기 계정의 시안만 조회됨 (타인 데이터 접근 불가)
- [ ] 로그아웃 버튼 작동

---

## 관리자 (07_Admin)

- [ ] 일반 사용자의 `/admin` 접근 차단 → 로그인 페이지 리디렉션
- [ ] 관리자 계정 접근 성공
- [ ] 전체 사용자 수·오늘 활동 사용자 수 표시
- [ ] 시안 생성 건수·이메일 발송 건수 통계 표시
- [ ] 사용자 목록 (가입일·이메일·시안 생성 횟수) 확인
- [ ] 기능 실행 로그 표 확인
- [ ] 오류 로그 확인
- [ ] 날짜 필터·성공/실패 필터 작동
- [ ] 화면에 API 키·토큰·비밀키가 표시되지 않음

---

## `[다음 단계]` Supabase 보안

- [ ] RLS(Row Level Security) 모든 테이블에 활성화
- [ ] 사용자 A가 사용자 B의 draft_inputs·drafts 조회 불가
- [ ] 클라이언트 코드에 service role key 없음
- [ ] 관리자 API가 서버에서 profiles.role 재확인
- [ ] email_delivery_logs 삽입이 서버 Route에서만 처리됨

---

## `[다음 단계]` Gemini / Stitch AI

- [ ] Gemini API 키가 환경변수로만 관리됨
- [ ] Gemini 응답 전문이 DB에 저장되지 않음 (요약만 저장)
- [ ] Gemini API 할당량 초과 시 오류 처리
- [ ] Stitch AI 시안 생성 실패 시 오류 메시지 표시
- [ ] 생성된 시안 HTML이 drafts 테이블에 정상 저장됨

---

## `[다음 단계]` Gmail 발송 (Agentria API)

- [ ] Agentria API 키가 환경변수로만 관리됨
- [ ] 발송 성공 시 email_delivery_logs.status = 'sent'
- [ ] 발송 실패 시 email_delivery_logs.status = 'failed' + error_code 저장
- [ ] 이메일 본문·제목이 DB에 저장되지 않음

---

## `[다음 단계]` 배포

- [ ] GitHub 최신 코드 Push
- [ ] Vercel 빌드 성공
- [ ] Vercel 환경변수 등록 (GEMINI_API_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, AGENTRIA_API_KEY, NEXT_PUBLIC_APP_URL)
- [ ] Supabase 허용 도메인에 배포 URL 등록
- [ ] 실제 배포 주소에서 로그인 테스트
- [ ] 실제 배포 주소에서 시안 생성 테스트
- [ ] 실제 배포 주소에서 이메일 발송 테스트
