# TEST_CHECKLIST.md — 한다뚝딱 (handa. 뚝딱)

> 이 문서의 모든 항목은 실제 연동(Google 로그인, Gemini API, Supabase Database/Storage, Gmail 발송)을 기준으로 확인합니다. Stitch AI는 MCP 전용·비공식이라 보류하고, 시안 HTML도 Gemini로 생성합니다.
>
> **2026-08-03 QA 진행**: 아래 체크된 항목은 로컬 dev 서버(`localhost:3000`)에서 브라우저로 직접 확인. 실제 Google OAuth 로그인 자체는 자격증명 입력이 필요해 자동화 도구로 수행하지 않았으며, 코드 리뷰로 실연동만 확인함(`app/api/auth/google/route.js`가 `supabase.auth.signInWithOAuth` 실호출). 나머지 플로우는 기존 더미 로그인 버튼으로 세션을 얻어 테스트함. 미체크 항목은 시간 관계상 미실시.

---

## 프로젝트 실행

- [x] 의존성 설치 성공 (`npm install`)
- [x] 개발 서버 실행 성공 (`npm run dev`)
- [x] 콘솔에 API 키 등 비밀값이 출력되지 않음
- [x] `npm run build` 성공 (빌드 오류 없음) — 2026-08-03 확인, 16개 라우트 정상 생성
- [x] `.env` 파일이 `.gitignore`에 포함되어 있음 (`.env*`)

---

## 로그인 (01_Login)

> **2026-08-03 추가 QA**: 이메일/비밀번호 로그인과 회원가입을 더미에서 실제 Supabase Auth(`signInWithPassword`/`signUp`)로 전환. 관리자 API(service role)로 이메일 확인이 완료된 테스트 계정을 만들어 로그인 성공 케이스까지 직접 검증했고, 테스트 계정은 검증 직후 삭제함.

- [x] Google 로그인 버튼 클릭 시 실제 Supabase Auth 로그인 성공 — 코드 확인(`signInWithOAuth`), 실제 OAuth 클릭은 자격증명 문제로 미실시
- [x] 이메일/비밀번호 로그인 버튼 클릭 시 실제 Supabase Auth 로그인 성공 — 확인 완료 테스트 계정으로 직접 로그인 성공 확인
- [x] 이메일/비밀번호 회원가입 시 실제 Supabase Auth 계정 생성 — 실제 가입 후 `auth.users`에 생성 확인(이메일 확인 필요 상태로 `/login?notice=check_email`로 이동)
- [x] 로그인 실패 시 오류 메시지 표시 — 잘못된 비밀번호로 "이메일 또는 비밀번호가 올바르지 않습니다." 확인
- [x] 회원가입 유효성 검사(비밀번호 불일치·6자 미만·중복 이메일) — 비밀번호 불일치 케이스 실제 확인, 나머지는 코드 확인
- [ ] 로그인 중 버튼 중복 클릭 방지 — 미실시
- [x] 로그인 성공 후 홈(02_Home)으로 자동 이동
- [x] 로그아웃 작동
- [x] 재방문 시 세션 유지 (자동 로그인)
- [x] 비로그인 상태에서 `/input`, `/mypage`, `/admin` 접근 시 로그인 페이지로 리디렉션 (`/result`는 draftId 필요해 미실시)

---

## 정보 입력 (03_Input)

폼은 3단계 위저드다: 1단계 기본 정보(업체명, 지역/업종, 이메일) → 2단계 매장 자랑거리(강조 제품, 장점, 사진 선택) → 3단계 디자인 스타일(색상, 분위기) → 제출.

- [x] 각 단계에서 "다음" 클릭 시 그 단계의 필수 항목만 검증, 미입력 시 다음 단계로 못 넘어감
- [x] 미입력 필드에 강조 표시 + 안내 메시지 출력 ("필수 입력 항목입니다.")
- [x] "이전" 버튼으로 앞 단계 복귀, 입력값 유지
- [x] 색상 테마 8가지 중 1개만 선택 가능 (radio)
- [x] 홈페이지 분위기 5가지 중 1개만 선택 가능 (radio)
- [ ] 매장 사진 1~3장 업로드 및 미리보기 표시 (선택 항목) — 미실시(선택 항목이라 스킵)
- [ ] 사진 업로드 실패 시 안내 메시지 표시 — 미실시
- [x] 입력값 + 현재 단계 임시 저장 (새로고침 후에도 유지) — localStorage 정상 동작 확인
- [ ] 이메일 형식 검증 (잘못된 형식 입력 시 경고) — 미실시(type=email 네이티브 검증만 확인)
- [x] 3단계에서 "AI 시안 만들기" 클릭 시 전체 필수값 재검증 후 제출

---

## AI 처리 / 로딩 (04_Loading)

- [x] 제출 후 로딩 화면으로 전환
- [x] 단계별 진행 메시지 표시
- [x] Gemini API 실제 호출로 운영 제안 문구 생성 성공 (POST /api/generate 200, 약 77초 소요)
- [x] Gemini API 실제 호출로 시안 HTML 생성 성공
- [ ] 오류 상황 발생 시 오류 메시지 + 재시도 버튼 표시 — 미실시
- [x] 처리 완료 시 결과 화면(05_Result)으로 자동 이동
- [ ] 취소 버튼 클릭 시 입력 화면으로 복귀 — 미실시

---

## 시안 결과 (05_Result)

- [x] 생성된 홈페이지 시안이 미리보기로 표시됨 (`/result/[draftId]/preview`, 로그인 불필요)
- [x] 운영 제안 문구 카드 표시
- [x] "이메일로 받기" 버튼 클릭 시 Gmail(Resend API) 실제 발송 요청
- [x] 이메일 발송 성공 안내 메시지 표시 ("이메일 발송에 성공했습니다.")
- [ ] 이메일 발송 실패 시 안내 메시지 + 재시도 버튼 표시 — 미실시
- [ ] 발송 실패 후에도 시안이 마이페이지에 보존됨 — 미실시
- [x] "마이페이지에 저장" 버튼 작동
- [ ] "다시 만들기" 버튼 클릭 시 입력 화면(03_Input)으로 이동 — 미실시
- [x] DB에 이메일 본문이 저장되지 않음 (관리자 이메일 로그에 상태/시각/오류코드만 표시됨을 확인)
- [x] email_delivery_logs에 상태(status)만 저장됨

---

## 마이페이지 (06_MyPage)

- [x] 로그인한 사용자의 이메일 표시
- [x] 이전 생성 시안 히스토리 목록 표시
- [x] 히스토리 항목에 업종·생성일시·이메일 발송 여부 표시
- [x] 시안 항목 클릭 시 결과 화면(05_Result)으로 이동 (링크 확인, 클릭 미실시)
- [x] 이메일 재발송 버튼 작동 ("재발송에 성공했습니다.")
- [x] 자기 계정의 시안만 조회됨 (타인 데이터 접근 불가) — `getDraftsByUser(userId)`가 `user_id` 명시 필터링(코드 확인)
- [x] 로그아웃 버튼 작동

---

## 관리자 (07_Admin)

- [x] 일반 사용자의 `/admin` 접근 차단 → 홈으로 리디렉션 (비로그인 시 로그인 페이지로 리디렉션도 확인)
- [x] 관리자 계정 접근 성공 ("(테스트용) 관리자로 로그인"으로 확인)
- [x] 전체 사용자 수·오늘 활동 사용자 수 표시
- [x] 시안 생성 건수·이메일 발송 건수 통계 표시
- [x] 사용자 목록 (가입일·이메일·시안 생성 횟수) 확인 — 실제 Gmail 계정 다수 확인됨
- [ ] 기능 실행 로그 표 확인 — 미실시
- [ ] 오류 로그 확인 — 미실시
- [ ] 날짜 필터·성공/실패 필터 작동 — 미실시
- [x] 화면에 API 키·토큰·비밀키가 표시되지 않음 (이메일 로그에 상태/시각/오류코드만 노출)

---

## Supabase 보안

- [ ] RLS(Row Level Security) 모든 테이블에 활성화 — 2026-07-29 세션에 확인, 오늘 재검증 안 함
- [x] 사용자 A가 사용자 B의 draft_inputs·drafts 조회 불가 — `getDraftsByUser(userId)` 서버 측 `user_id` 필터 코드 확인(오늘)
- [x] 클라이언트 코드에 service role key 없음 — `app/` 전체 `grep SERVICE_ROLE` 결과 없음(오늘 확인)
- [x] 관리자 API가 서버에서 profiles.role 재확인 — `app/admin/page.js`에서 `profile.role !== "admin"` 서버측 재확인(오늘)
- [ ] email_delivery_logs 삽입이 서버 Route에서만 처리됨 — 오늘 재검증 안 함

---

## Gemini API

- [ ] Gemini API 키가 환경변수로만 관리됨 — 오늘 재검증 안 함
- [ ] Gemini 응답 전문이 DB에 저장되지 않음 (요약만 저장) — 오늘 재검증 안 함
- [ ] Gemini API 할당량 초과 시 오류 처리 — 미실시
- [ ] Gemini가 유효한 HTML을 생성하지 못했을 때 오류 메시지 표시 — 미실시
- [x] 생성된 시안 HTML이 drafts 테이블에 정상 저장됨 — 오늘 새 시안 생성 후 `/result/[draftId]/preview`에서 정상 렌더 확인

---

## Gmail 발송 (Resend API)

- [ ] Resend API 키가 환경변수로만 관리됨 — 오늘 재검증 안 함
- [x] 발송 성공 시 email_delivery_logs.status = 'sent' — 오늘 발송 성공 후 관리자 이메일 로그에 "발송 성공"으로 표시됨
- [ ] 발송 실패 시 email_delivery_logs.status = 'failed' + error_code 저장 — 과거(07-29) validation_error 기록 있음, 오늘 재현 안 함
- [x] 이메일 본문·제목이 DB에 저장되지 않음 — 관리자 이메일 로그에 상태/시각/오류코드만 노출 확인

---

## 배포

- [ ] GitHub 최신 코드 Push — 오늘 코드 변경 없음(체크리스트 문서만 갱신)
- [ ] Vercel 빌드 성공 — 로컬 build만 확인, Vercel 재검증 안 함
- [ ] Vercel 환경변수 등록 — 오늘 재검증 안 함
- [ ] Supabase 허용 도메인에 배포 URL 등록 — 오늘 재검증 안 함
- [ ] 실제 배포 주소에서 로그인 테스트 — 오늘은 로컬 dev만 테스트
- [ ] 실제 배포 주소에서 시안 생성 테스트 — 오늘은 로컬 dev만 테스트
- [ ] 실제 배포 주소에서 이메일 발송 테스트 — 오늘은 로컬 dev만 테스트
