# UI_REFERENCE.md — 한다뚝딱 (handa. 뚝딱)

참고 이미지는 디자인 완성본이 아니라 방향을 설명하는 자료입니다.
이미지마다 적용 화면 / 필수 요소 / 유지할 기능 / 복사하지 않을 요소를 구분해서 작성합니다.

---

## 전체 디자인 방향

- 40~60대 소상공인도 첫 화면에서 바로 이해할 수 있는 구조
- 글자 크기는 중장년 사용자 기준으로 충분히 크게 (본문 최소 16px 이상)
- 강조 색상은 한 가지 (handa 브랜드 컬러 기준)
- 단계별 진행이 명확하게 보이는 스텝 인디케이터 사용
- 과도한 애니메이션 금지 — 필수 피드백(로딩, 성공, 오류)만 사용
- 버튼은 크고 누르기 쉽게, 터치 영역 최소 48px
- 전문 용어, 영어 약어 최소화 — 소상공인 언어로 작성

---

## 와이어프레임

파일: references/wireframe.png

이 이미지는 디자인 완성본이 아니라 페이지 구조와 이동 흐름을 설명한다.

흐름: 로그인 → 홈 → 정보 입력 → AI 처리(로딩) → 시안 미리보기 → 저장/이메일 발송 → 마이페이지

---

## 로그인 화면 (01_Login)

참고 이미지: references/login-reference.png

필수 요소:
- 한다뚝딱 로고
- 서비스 이름 및 한 줄 소개 ("사장님 홈페이지, 5분이면 뚝딱!")
- Google 로그인 버튼
- 개인정보 최소 수집 안내

유지할 기능:
- 로그인 실패 시 안내 메시지 표시
- 로그인 중 버튼 중복 클릭 방지
- 로그인 성공 시 홈으로 자동 이동

복사하지 않을 요소: 타사 로고, 서비스명, 이미지 속 가짜 데이터

---

## 홈 화면 (02_Home)

참고 이미지: references/home-reference.png

필수 요소:
- 상단 네비게이션 (로고, 마이페이지 링크)
- 메인 헤드라인 ("사장님 홈페이지, 5분이면 뚝딱!")
- 서비스 흐름 설명 (3단계 아이콘: 정보입력 → AI생성 → 이메일수신)
- 샘플 시안 예시 이미지 (업종별 2~3종)
- 시작하기 CTA 버튼 (크고 명확하게)
- 간단한 서비스 소개 문구

유지할 기능:
- 로그인 상태에 따라 CTA 버튼 분기 (비로그인 → 로그인 유도 / 로그인 → 바로 시작)
- 샘플 시안 클릭 시 미리보기 가능

복사하지 않을 요소: 타사 로고, 서비스명, 이미지 속 가짜 데이터

---

## 정보 입력 화면 (03_Input)

참고 이미지: references/input-reference.png

필수 요소:
- 상단 스텝 인디케이터: 앱 전체 흐름(입력→AI생성→결과확인)과는 별개로, 입력 폼 자체가 3단계 위저드로 나뉘어 있다 ("정보 입력 1/3" 형식으로 표시해 앱 전체 단계 표기와 겹치지 않게 한다)
- 입력 항목 7개, 3단계로 분할:
  - 1단계 기본 정보: 업체명 — 필수, 지역/업종 — 필수, 이메일 주소(시안 수신용) — 필수
  - 2단계 매장 자랑거리: 강조 제품 및 메인 문구 — 필수, 회사/매장 장점(자유 입력) — 필수, 매장 사진 업로드(1~3장) — 선택
  - 3단계 디자인 스타일: 색상 조합 선택(8가지 프리셋) — 필수, 홈페이지 분위기 선택(5가지) — 필수 → 여기서 "AI 시안 만들기" 제출
- SNS 주소·사업자등록번호·벤치마킹 사이트·연락처·추가 요구사항은 더 이상 입력받지 않는다 (실제 AI 생성에 쓰이지 않아 제거)

유지할 기능:
- 필수 항목 미입력 시 해당 필드 강조 + 안내 메시지, "다음" 버튼은 현재 단계 필수값만 검증
- 색상/분위기는 선택 UI (라디오 버튼 or 카드 선택)
- 사진 업로드 미리보기
- 입력값 + 현재 단계 임시 저장 (새로고침 시 유지)
- "이전" 버튼으로 앞 단계로 돌아가기

복사하지 않을 요소: 타사 로고, 서비스명, 이미지 속 가짜 데이터

---

## 로딩 화면 (04_Loading)

필수 요소:
- 진행 상태 표시 (단계별 메시지)
  - "입력 내용을 분석하고 있어요..."
  - "홈페이지 구조를 설계하고 있어요..."
  - "문구와 디자인을 완성하고 있어요..."
- 진행률 바 또는 스피너
- 취소 버튼 (처리 중단 가능)

유지할 기능:
- API 오류 발생 시 오류 메시지 + 재시도 버튼 표시
- 처리 완료 시 자동으로 결과 화면 이동

복사하지 않을 요소: 타사 로딩 애니메이션, 브랜드 이미지

---

## 결과(시안 미리보기) 화면 (05_Result)

참고 이미지: references/result-reference.png

필수 요소:
- 생성된 홈페이지 시안 미리보기 (iframe 또는 이미지)
- AI가 작성한 운영 제안 문구 카드
- 이메일 발송 버튼 ("이메일로 받기")
- 저장 버튼 ("마이페이지에 저장")
- 다시 만들기 버튼 (입력 화면으로 돌아가기)

유지할 기능:
- 이메일 발송 성공/실패 안내
- 저장 성공 안내
- 발송 중 버튼 중복 클릭 방지
- 시안은 이메일 발송 실패 시에도 마이페이지에 보존

복사하지 않을 요소: 타사 로고, 서비스명, 이미지 속 가짜 데이터

---

## 마이페이지 (06_MyPage)

필수 요소:
- 사용자 이메일
- 생성한 시안 히스토리 목록 (카드 형태)
  - 업종명
  - 생성 일시
  - 이메일 발송 여부
- 시안 다시보기 버튼
- 이메일 재발송 버튼
- 로그아웃 버튼

유지할 기능:
- 시안 클릭 시 결과 화면으로 이동
- 이메일 재발송 성공/실패 안내

복사하지 않을 요소: 타사 로고, 서비스명, 이미지 속 가짜 데이터

---

## 관리자 화면 (07_Admin)

참고 이미지: references/admin-reference.png

필수 요소:
- 전체 사용자 수
- 오늘 활동 사용자 수
- 시안 생성 건수 / 이메일 발송 건수 통계
- 사용자 목록 (가입일, 이메일, 시안 생성 횟수)
- 기능 실행 로그 표 (생성 요청, 발송 요청, 성공/실패)
- 오류 로그 표
- 날짜 필터 / 성공·실패 필터

유지할 기능:
- 일반 사용자에게 관리자 메뉴 표시하지 않음
- 서버에서 권한 확인 전 데이터 불러오지 않음
- 날짜/상태 필터 적용

복사하지 않을 요소: 타사 로고, 서비스명, 이미지 속 가짜 데이터

---

## 이미지 사용 규칙

- 타사 로고와 브랜드명을 복사하지 않는다
- 참고 이미지의 사진을 그대로 가져오지 않는다
- 저작권이 불명확한 매장 사진은 사용하지 않는다
- 필요하면 직접 만든 플레이스홀더 이미지를 사용한다
- 참고 이미지에서 레이아웃과 정보 구조만 참고한다


